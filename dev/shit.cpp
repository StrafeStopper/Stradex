//this is an extra file for putting joke stuff in
//it wont make it into the final build beacause I don't want to get in trouble with the rating people
//just some fun stuff for the devs to play with 

/*
//creates a swastika over the screen
SDL_Rect top = { 240, 50, 200, 20 };
SDL_Rect up = { 240, 50, 20, 380 };
SDL_Rect right = { 240, 240, 200, 20 };
SDL_Rect rightdown = { 430, 240, 20, 190 };
SDL_Rect left = { 40, 240, 200, 20 };
SDL_Rect bottom = { 40, 420, 220, 20 };
SDL_Rect leftup = { 40, 50, 20, 190 };
SDL_SetRenderDrawColor(renderer, 0, 0, 0, 255);
SDL_RenderFillRect(renderer, &top);
SDL_RenderFillRect(renderer, &up);
SDL_RenderFillRect(renderer, &right);
SDL_RenderFillRect(renderer, &rightdown);
SDL_RenderFillRect(renderer, &left);
SDL_RenderFillRect(renderer, &bottom);
SDL_RenderFillRect(renderer, &leftup);
*/
