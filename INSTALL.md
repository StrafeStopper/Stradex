# Install

This engine has no install client

Run `install.sh` to install the needed dependencies to compile the program in linux

If the script doesn't automatically compile the program then refer to the next line

To compile in linux run `make linux` and use `./light` to run the engine

(Not working just yet)To compile in windows run `make windows` and use `wine light.exe` or simply run the program if you are on windows
